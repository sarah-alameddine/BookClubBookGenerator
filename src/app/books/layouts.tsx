import "../globals.css"; // optional if already in root layout

export default function BooksLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <div>{/* TODO: NOT BEING USD YET  */}</div>;
}
